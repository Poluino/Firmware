#include <Arduino.h>
#include "SPIFFS.h"
 
void setup() {
  Serial.begin(115200);
  
  if(!SPIFFS.begin(true)){
    Serial.println("An Error has occurred while mounting SPIFFS");
    return;
  }
  //File file = SPIFFS.open("/config.json");
  Serial.println(SPIFFS.remove("/config.json"));
  Serial.println(SPIFFS.exists("/config.json"));

}

 
void loop() {

}